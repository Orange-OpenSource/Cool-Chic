Utils
=====

``Utils`` contains helper functions used to manage the encoding.

   * ``CodingStructure`` is an abstraction representing how a video is encoded
     *i.e.* intra period, hierarchical GOP etc.

   * ``Preset`` is used to define different training (encoding) processes

.. toctree::

   CodingStructure <codingstructure>
   Preset <preset>
